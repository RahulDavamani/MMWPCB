model ProductFabricationStatus {
   id        String   @id @default(nanoid())
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt

   num         Int
   status      String
   completedAt DateTime?

   standardPcbId String?
   standardPcb   StandardPcb? @relation(fields: [standardPcbId], references: [id])

   advancedPcbId String?
   advancedPcb   AdvancedPcb? @relation(fields: [advancedPcbId], references: [id])

   flexiblePcbId String?
   flexiblePcb   FlexiblePcb? @relation(fields: [flexiblePcbId], references: [id])

   rigidFlexId String?
   rigidFlex   RigidFlex? @relation(fields: [rigidFlexId], references: [id])

   assemblyId String?
   assembly   Assembly? @relation(fields: [assemblyId], references: [id])

   stencilId String?
   stencil   Stencil? @relation(fields: [stencilId], references: [id])

   cncId String?
   cnc   CNC?    @relation(fields: [cncId], references: [id])

   sheetMetalId String?
   sheetMetal   SheetMetal? @relation(fields: [sheetMetalId], references: [id])

   threePrintingId String?
   threePrinting   ThreePrinting? @relation(fields: [threePrintingId], references: [id])

   injectionMoldingId String?
   injectionMolding   InjectionMolding? @relation(fields: [injectionMoldingId], references: [id])

   vacuumCastingId String?
   vacuumCasting   VacuumCasting? @relation(fields: [vacuumCastingId], references: [id])
}
